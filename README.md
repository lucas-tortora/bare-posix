# bare-posix

POSIX-specific bindings for Bare.

```
npm i bare-posix
```

## API

#### `posix.getgid()`

#### `posix.getegid()`

#### `posix.getuid()`

#### `posix.geteuid()`

#### `posix.getgroups()`

## License

Apache-2.0
